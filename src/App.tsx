import { useState } from 'react'
import { Bell, CalendarDays, Check, ChevronDown, CircleHelp, Clock3, FileText, LayoutDashboard, LogOut, Menu, MessageSquare, MoreHorizontal, Plus, Search, Settings, ShieldCheck, Users, X } from 'lucide-react'
import './App.css'

type View = 'Overview' | 'Calendar' | 'People' | 'Attendance' | 'Leave' | 'Messages' | 'Reports' | 'Settings'

const navItems: { label: View; icon: typeof LayoutDashboard }[] = [
  { label: 'Overview', icon: LayoutDashboard }, { label: 'Calendar', icon: CalendarDays }, { label: 'People', icon: Users }, { label: 'Attendance', icon: Clock3 }, { label: 'Leave', icon: FileText }, { label: 'Messages', icon: MessageSquare }, { label: 'Reports', icon: ShieldCheck },
]
const events = [{ day: '18', month: 'SEP', title: 'RTL design sync', time: '09:30 - 10:15', tag: 'PROJECT · APOLLO', color: 'cyan' }, { day: '18', month: 'SEP', title: 'Verification review', time: '14:00 - 15:00', tag: 'PROJECT · ATLAS', color: 'yellow' }, { day: '19', month: 'SEP', title: 'Leave policy check-in', time: '11:30 - 12:00', tag: 'PEOPLE OPS', color: 'pink' }]
const people = [{ initials: 'FA', name: 'Foez Ahmed', role: 'Administrator', status: 'Active', team: 'Leadership', tone: 'lime' }, { initials: 'RA', name: 'Rafi Ahmed', role: 'Verification Lead', status: 'Active', team: 'Engineering', tone: 'cyan' }, { initials: 'NS', name: 'Nusrat Sultana', role: 'RTL Engineer', status: 'Pending', team: 'Engineering', tone: 'pink' }]

function App() {
  const [activeView, setActiveView] = useState<View>('Overview')
  const [sidebarOpen, setSidebarOpen] = useState(false)
  const [showInvite, setShowInvite] = useState(false)
  const [query, setQuery] = useState('')
  const [checkedIn, setCheckedIn] = useState(false)
  const pageTitle = activeView === 'Overview' ? 'Good morning, Foez' : activeView
  const pageDescription = activeView === 'Overview' ? 'Here is your team pulse for Thursday, 18 September 2026.' : `Manage ${activeView.toLowerCase()} across ADN Semiconductors.`

  return (
    <div className="app-shell">
      <aside className={`sidebar ${sidebarOpen ? 'is-open' : ''}`}>
        <div className="brand"><span className="brand-mark">A</span><span>ADN <b>EMS</b></span></div><div className="workspace-label">WORKSPACE <span>OPS / 01</span></div>
        <nav aria-label="Main navigation">{navItems.map(({ label, icon: Icon }) => <button key={label} className={activeView === label ? 'nav-item active' : 'nav-item'} onClick={() => { setActiveView(label); setSidebarOpen(false) }}><Icon size={17} /><span>{label}</span>{label === 'Messages' && <em>3</em>}</button>)}</nav>
        <div className="sidebar-bottom"><button className={activeView === 'Settings' ? 'nav-item active' : 'nav-item'} onClick={() => setActiveView('Settings')}><Settings size={17} /><span>Settings</span></button><div className="user-card"><div className="avatar lime">FA</div><div><strong>Foez Ahmed</strong><span>Administrator</span></div><ChevronDown size={15} /></div><button className="sign-out"><LogOut size={15} /> Sign out</button></div>
      </aside>
      <main className="main-content"><header className="topbar"><button className="icon-button mobile-menu" aria-label="Open navigation" onClick={() => setSidebarOpen(true)}><Menu size={20} /></button><div className="breadcrumb"><span>ADN EMS</span><b>/</b><strong>{activeView}</strong></div><div className="top-actions"><button className="icon-button" aria-label="Help"><CircleHelp size={19} /></button><button className="icon-button notification" aria-label="Notifications"><Bell size={19} /><i /></button><div className="top-avatar">FA</div></div></header>
        <div className="content-wrap"><section className="page-heading"><div><p className="eyebrow">THURSDAY / 18 SEP 2026</p><h1>{pageTitle}</h1><p className="subheading">{pageDescription}</p></div><div className="heading-actions"><button className="button ghost" onClick={() => setActiveView('Calendar')}><CalendarDays size={16} /> View calendar</button><button className="button primary" onClick={() => setShowInvite(true)}><Plus size={17} /> Add teammate</button></div></section>
          {activeView === 'Overview' ? <><section className="metric-grid"><div className="metric-card metric-dark"><div className="metric-top"><span>TEAM CAPACITY</span><span className="metric-arrow">↗</span></div><strong>82<span>%</span></strong><div className="metric-footer"><span className="positive">+6.4%</span><span>vs last month</span></div><div className="sparkline"><i /><i /><i /><i /><i /><i /><i /><i /></div></div><div className="metric-card"><div className="metric-top"><span>ACTIVE PEOPLE</span><Users size={16} /></div><strong>24</strong><div className="metric-footer"><span className="positive">+3</span><span>this quarter</span></div><div className="avatar-stack"><div className="avatar cyan">RA</div><div className="avatar yellow">NS</div><div className="avatar pink">MA</div><div className="avatar neutral">+21</div></div></div><div className="metric-card"><div className="metric-top"><span>OPEN REQUESTS</span><Clock3 size={16} /></div><strong>08</strong><div className="metric-footer"><span className="warning">3 urgent</span><span>needs review</span></div><div className="progress"><span style={{ width: '62%' }} /></div></div><div className="metric-card metric-accent"><div className="metric-top"><span>YOUR ATTENDANCE</span><Check size={16} /></div><strong>{checkedIn ? 'In' : 'Out'}</strong><button className="metric-action" onClick={() => setCheckedIn(!checkedIn)}>{checkedIn ? 'Checked in at 09:12' : 'Check in for today'} <span>→</span></button></div></section><section className="dashboard-grid"><div className="panel schedule-panel"><div className="panel-heading"><div><p className="eyebrow">UP NEXT</p><h2>Today’s schedule</h2></div><button className="text-button" onClick={() => setActiveView('Calendar')}>See calendar <span>→</span></button></div><div className="event-list">{events.map((event) => <div className="event-row" key={event.title}><div className={`date-block ${event.color}`}><strong>{event.day}</strong><span>{event.month}</span></div><div className="event-detail"><strong>{event.title}</strong><span>{event.time}</span></div><span className={`event-tag ${event.color}`}>{event.tag}</span><button className="more-button" aria-label={`More options for ${event.title}`}><MoreHorizontal size={18} /></button></div>)}</div></div><div className="panel activity-panel"><div className="panel-heading"><div><p className="eyebrow">LIVE FEED</p><h2>Recent activity</h2></div><button className="more-button" aria-label="More activity options"><MoreHorizontal size={18} /></button></div><div className="activity-list"><div><span className="activity-dot lime" /><p><strong>Rafi Ahmed</strong> approved a pull request <b>rtl-core #184</b><small>12 minutes ago</small></p></div><div><span className="activity-dot cyan" /><p><strong>Nusrat Sultana</strong> requested leave for <b>24 Sep</b><small>41 minutes ago</small></p></div><div><span className="activity-dot pink" /><p><strong>You</strong> updated the <b>Atlas project</b><small>2 hours ago</small></p></div></div></div></section><section className="lower-grid"><div className="panel requests-panel"><div className="panel-heading"><div><p className="eyebrow">NEEDS YOUR ATTENTION</p><h2>Pending requests</h2></div><span className="count-badge">03</span></div>{people.filter(person => person.status === 'Pending').map(person => <div className="request-row" key={person.name}><div className={`avatar ${person.tone}`}>{person.initials}</div><div><strong>{person.name}</strong><span>Account approval · {person.team}</span></div><button className="approve-button" onClick={() => alert(`${person.name} approved`)}><Check size={15} /> Approve</button><button className="more-button" aria-label="More options"><MoreHorizontal size={18} /></button></div>)}<div className="request-row"><div className="request-icon"><FileText size={17} /></div><div><strong>Leave request · Rafi Ahmed</strong><span>18 Sep - 19 Sep · 2 days</span></div><button className="review-button" onClick={() => setActiveView('Leave')}>Review <span>→</span></button></div></div><div className="panel focus-panel"><div className="panel-heading"><div><p className="eyebrow">TEAM FOCUS</p><h2>Project allocation</h2></div><button className="more-button" aria-label="More project options"><MoreHorizontal size={18} /></button></div><div className="allocation"><div className="allocation-label"><span>APOLLO / RISC-V</span><b>64%</b></div><div className="allocation-bar"><span className="cyan-fill" style={{ width: '64%' }} /></div></div><div className="allocation"><div className="allocation-label"><span>ATLAS / VERIFICATION</span><b>48%</b></div><div className="allocation-bar"><span className="yellow-fill" style={{ width: '48%' }} /></div></div><div className="allocation"><div className="allocation-label"><span>INTERNAL / ACADEMY</span><b>26%</b></div><div className="allocation-bar"><span className="pink-fill" style={{ width: '26%' }} /></div></div><div className="focus-footer"><span><i className="live-dot" /> 6 projects active</span><button className="text-button" onClick={() => setActiveView('Reports')}>View report <span>→</span></button></div></div></section></> : <section className="panel placeholder-view"><div className="placeholder-icon"><Search size={21} /></div><p className="eyebrow">WORKSPACE MODULE</p><h2>{activeView}</h2><p>This module is ready for the Firebase data layer. Your role and permissions will determine the records shown here.</p><div className="search-line"><Search size={16} /><input aria-label={`Search ${activeView}`} placeholder={`Search ${activeView.toLowerCase()}...`} value={query} onChange={(event) => setQuery(event.target.value)} /></div></section>}
        </div></main>
      {showInvite && <div className="modal-backdrop"><div className="modal" role="dialog" aria-modal="true" aria-labelledby="invite-title"><button className="modal-close" aria-label="Close" onClick={() => setShowInvite(false)}><X size={18} /></button><p className="eyebrow">TEAM ACCESS</p><h2 id="invite-title">Add a teammate</h2><p>New accounts begin as pending until an administrator approves access.</p><label>GitHub username<input placeholder="e.g. github-handle" autoFocus /></label><label>Role<select defaultValue="employee"><option value="employee">Employee</option><option value="manager">Manager</option></select></label><button className="button primary full" onClick={() => setShowInvite(false)}>Send invitation <span>→</span></button></div></div>}
    </div>
  )
}

export default App
